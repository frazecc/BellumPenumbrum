import { getAccessToken, getCurrentUser, signOut, usernameFromEmail } from './auth.js';
const API='https://bellum-penumbrum-api.onrender.com', $=id=>document.getElementById(id),cache=new Map();
let state=null,matchId=null,busy=false,initialized=false,view=null,flow=null;
const board=()=>state?.board?.rows??[[null,null,null],[null,null,null],[null,null,null]],at=p=>board()[p.row]?.[p.col]??null;
const me=()=>state?.players?.[1],them=()=>state?.players?.[0],turn=()=>state?.status==='running'&&state.active_player_index===1&&state.phase==='main';
const pending=()=>state?.pending_mostrissimo?.player_index===1?state.pending_mostrissimo:null,active=()=>turn()&&!busy&&!pending();
const inside=p=>p.row>=0&&p.row<3&&p.col>=0&&p.col<3;
const around=p=>[{row:p.row-1,col:p.col},{row:p.row+1,col:p.col},{row:p.row,col:p.col-1},{row:p.row,col:p.col+1}].filter(inside);
const eq=(a,b)=>!!(a&&b&&a.row===b.row&&a.col===b.col),foes=p=>around(p).filter(q=>at(q)?.owner_index===0),steps=p=>around(p).filter(q=>q.row!==0&&!at(q));
const escape=x=>String(x??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#39;');
const creature=d=>['monster','mostrissimo'].includes(d.card_type),effects=d=>Array.isArray(d?.effect_json?.effects)?d.effect_json.effects:d?.effect_json?.type?[d.effect_json]:[];
const targetEffect=d=>effects(d).find(e=>e?.target==='any_creature'||e?.type==='return_hand');
const factions=['','CHI','INF','PES','BUL','GRO','CLO','IND'],types={monster:'MOSTRO',mostrissimo:'MOSTRISSIMO',sorcery:'STREGONERIA',instant:'ISTANTANEO',terraforma:'TERRAFORMA',aura:'AURA'},rarities={common:'Comune',uncommon:'Non comune',rare:'Rara',ultra_rare:'Ultra rara',legendary:'Leggendaria'};
const faction=d=>{const code=String(d?.faction_code??factions[Number(d?.faction_id)]??'IND').toLowerCase();return ['chi','inf','pes','bul','gro','clo','ind'].includes(code)?code:'ind';};
function cardHTML(d,mini=false,cell=null){const cls=`faction-${faction(d)}`,cost=d.card_type==='mostrissimo'?`✦${Number(d.sacrifice_cost??0)}`:`⚡${Number(d.mana_cost??0)}`,art=d.image_url?`<img src="${escape(d.image_url)}" alt="Illustrazione di ${escape(d.name)}" loading="lazy">`:'<span class="game-card-art-placeholder">🎴</span>',atk=cell?.attack??d.attack??0,hp=cell?.hp??d.hp??0;
 if(mini)return `<article class="board-card ${cls}"><header class="board-card-titlebar"><span class="board-card-name">${escape(d.name)}</span><span class="board-card-cost">${cost}</span></header><div class="board-card-art">${art}</div><div class="board-card-type">${escape(types[d.card_type]??d.card_type)}</div><div class="board-card-effect">${escape(String(d.effect_text??'').replace(/\*\*/g,'').replace(/\s+/g,' ').slice(0,90))}</div><footer class="board-card-footer"><span>⚔ ${atk}</span><span>❤ ${hp}</span></footer></article>`;
 const rules=escape(d.effect_text||'Nessun effetto.').replace(/\*\*(.+?)\*\*/g,'<strong>$1</strong>').replace(/\n/g,'<br>');return `<article class="game-card ${cls}"><header class="game-card-titlebar"><span class="game-card-name">${escape(d.name)}</span><span class="game-card-cost">${cost}</span></header><div class="game-card-art">${art}</div><div class="game-card-type-row"><span>${escape(types[d.card_type]??d.card_type)}</span><span class="game-card-subtype">${escape(d.subtype??'')}</span></div><div class="game-card-rules">${rules}</div><div class="game-card-flavor">${escape(d.flavor_text??'')}</div><footer class="game-card-footer"><span class="game-card-rarity">${escape(rarities[d.rarity]??d.rarity??'Comune')}</span>${creature(d)?`<span class="game-card-stats"><span class="game-card-atk">⚔ ${atk}</span><span class="game-card-hp">❤ ${hp}</span></span>`:''}</footer></article>`;}
function targetAllowed(d,c){if(!c)return false;if(d.card_type==='aura')return c.owner_index===1;const e=targetEffect(d);if(!e)return false;if(['damage','damage_creature'].includes(e.type)&&e.timing!=='instant')return c.owner_index===0;if(e.type==='heal'&&e.timing!=='instant')return c.owner_index===1;return true;}
function targets(d){const found=[];for(let r=0;r<3;r++)for(let col=0;col<3;col++){const cell=at({row:r,col});if(targetAllowed(d,cell))found.push(cell);}return found;}
function permanents(){const found=[];for(let r=0;r<3;r++)for(let col=0;col<3;col++){const c=at({row:r,col});if(c?.owner_index!==1)continue;found.push({id:c.instance_id,card_id:c.card_id,type:'Creatura'});for(const a of c.auras??[])found.push({id:a.instance_id,card_id:a.card_id,type:'Aura'});}if(me()?.field_spell)found.push({id:me().field_spell.instance_id,card_id:me().field_spell.card_id,type:'Terraforma'});return found;}
function bossCells(p){if(!p)return[];const found=[];for(let r=0;r<3;r++)for(let col=0;col<3;col++){const pos={row:r,col};if(!at(pos)&&(r===2||(r===1&&(p.freed_positions??[]).some(x=>eq(x,pos)))))found.push(pos);}return found;}
function notice(text,type=''){if($('game-message')){$('game-message').textContent=text;$('game-message').className=`game-message ${type}`;}}
function fail(e){console.error(e);notice(e instanceof Error?e.message:'Errore inatteso','error');}
function close(){view=null;$('card-detail-overlay')?.classList.add('hidden');document.body.classList.remove('detail-open');}
async function api(path,options={}){const token=await getAccessToken();if(!token)throw new Error('Sessione scaduta, accedi di nuovo.');const abort=new AbortController(),timer=setTimeout(()=>abort.abort(),45000);try{const res=await fetch(`${API}${path}`,{method:options.method??'GET',headers:{Authorization:`Bearer ${token}`,...(options.body!==undefined?{'Content-Type':'application/json'}:{})},body:options.body!==undefined?JSON.stringify(options.body):undefined,signal:abort.signal});const json=await res.json().catch(()=>({}));if(!res.ok)throw new Error(json.error??`HTTP ${res.status}`);return json;}catch(e){if(e?.name==='AbortError')throw new Error('Render non risponde entro 45 secondi.');throw e;}finally{clearTimeout(timer);}}
async function card(id){if(!cache.has(id))cache.set(id,(await api(`/cards/${encodeURIComponent(id)}`)).card);return cache.get(id);}
function button(text,fn,disabled=false){const b=document.createElement('button');b.type='button';b.className='primary-button';b.textContent=text;b.disabled=disabled;b.onclick=()=>Promise.resolve(fn()).catch(fail);return b;}
function overlay(){let el=$('card-detail-overlay');if(el)return el;el=document.createElement('div');el.id='card-detail-overlay';el.className='card-detail-overlay hidden';el.setAttribute('role','dialog');el.setAttribute('aria-modal','true');el.innerHTML='<div class="detail-stage"><div class="detail-image" id="detail-image"></div><div class="detail-actions" id="detail-actions"></div></div>';el.onclick=e=>{if(e.target===el)close();};document.body.append(el);return el;}
async function inspect(kind,id,extra={}){if(busy)return;view={kind,id,...extra};const current=view,d=await card(id);if(current!==view)return;overlay();$('detail-image').innerHTML=cardHTML(d,false,current.cell??null);const bar=$('detail-actions');bar.replaceChildren();const add=(text,fn,disabled=false)=>bar.append(button(text,fn,disabled));
 if(kind==='hand'&&active())add(d.card_type==='monster'?'Evoca':'Gioca',()=>beginCard(current.instanceId,d),Number(d.mana_cost)>me().current_mana);
 if(kind==='boss'&&active()&&state.last_mostrissimo_turn?.[1]!==state.current_turn){const cost=Number(d.sacrifice_cost),own=board().flat().filter(c=>c?.owner_index===1).length,legal=Number.isInteger(cost)&&cost>=0&&cost<=6&&permanents().length>=cost&&(board()[2].some(c=>!c)||(cost>0&&own>0));add(`Evoca · ${cost} sacrifici`,()=>request('mostrissimo/start',{cardId:id},'Evocazione iniziata: non puoi annullare.'),!legal);}
 if(kind==='unit'&&active()&&current.cell?.owner_index===1&&!current.cell.tired){add('Attacca · 0 mana',()=>{flow={kind:'attack',from:current.position};close();render().catch(fail);notice(foes(current.position).length?'Tocca il nemico evidenziato.':'Premi Attacca IA direttamente.','success');});add('Muovi · 1 mana',()=>{flow={kind:'move',from:current.position};close();render().catch(fail);notice('Tocca una cella libera adiacente.','success');},!me()?.current_mana||!steps(current.position).length);}
 if(kind==='tribute'&&pending()&&pending().paid.length<pending().required)add('Conferma sacrificio',()=>request('mostrissimo/sacrifice',{instanceId:current.instanceId},'Sacrificio risolto.'));
 add('Chiudi',close);$('card-detail-overlay').classList.remove('hidden');document.body.classList.add('detail-open');}
async function beginCard(instanceId,d){if(!active())return;close();flow={kind:'hand',instanceId,id:d.id,step:d.card_type==='monster'?'cell':(d.card_type==='aura'||targetEffect(d))?'target':'immediate'};if(flow.step==='immediate')return request('play-card',{cardInstanceId:instanceId,options:{}},'Carta giocata.');await render();notice(flow.step==='cell'?'Scegli una cella libera della tua riga. Puoi annullare.':'Scegli una creatura bersaglio.','success');}
async function drawBoard(){const root=$('shared-board');if(!root)return;root.replaceChildren();const p=pending(),legal=p?.paid.length===p?.required?bossCells(p):[],d=flow?.id?cache.get(flow.id):null;for(let r=0;r<3;r++){const line=document.createElement('div');line.className=`board-row ${['ai-row','center-row','human-row'][r]}`;for(let col=0;col<3;col++){const pos={row:r,col},c=at(pos),b=document.createElement('button');b.type='button';b.className='board-cell';let definition=null;if(c)try{definition=await card(c.card_id);}catch(e){console.warn(e);}b.classList.add(c?c.owner_index===1?'human-card':'ai-card':'empty');if(c?.tired)b.classList.add('tired');if(flow?.kind==='move'&&steps(flow.from).some(x=>eq(x,pos)))b.classList.add('valid-move');if(flow?.kind==='attack'&&foes(flow.from).some(x=>eq(x,pos)))b.classList.add('valid-target');if(flow?.kind==='hand'&&flow.step==='cell'&&r===2&&!c)b.classList.add('valid-summon');if(((flow?.kind==='hand'&&flow.step==='target')||flow?.kind==='boss-target')&&d&&targetAllowed(d,c))b.classList.add('valid-target');if(legal.some(x=>eq(x,pos)))b.classList.add('valid-summon');b.setAttribute('aria-label',c?`${definition?.name??'Creatura'} ${c.owner_index===1?'Tu':'IA'} ATK ${c.attack} HP ${c.hp}`:`Cella [${r},${col}]`);b.innerHTML=c&&definition?cardHTML(definition,true,c)+`<span class="cell-coordinate">[${r},${col}]</span>`:`<span class="empty-label">${['Riga IA','Centro','Riga Tu'][r]}<br>[${r},${col}]</span>`;b.onclick=()=>boardClick(pos).catch(fail);line.append(b);}root.append(line);}}
async function drawHand(){const root=$('player-hand');if(!root)return;root.replaceChildren();for(const inst of me()?.hand??[]){const b=document.createElement('button');b.type='button';b.className='hand-card';try{const d=await card(inst.card_id);b.innerHTML=cardHTML(d,true);b.disabled=!active();b.setAttribute('aria-label',`Apri ${d.name}`);b.onclick=()=>inspect('hand',inst.card_id,{instanceId:inst.instance_id});}catch(e){b.textContent='Carta non caricabile';b.disabled=true;console.warn(e);}root.append(b);}}
async function drawBoss(){let panel=$('mostrissimo-panel');if(!panel){panel=document.createElement('section');panel.id='mostrissimo-panel';panel.className='panel';panel.setAttribute('aria-label','Offerta Mostrissimi');const before=document.querySelector('.logs-panel')??$('game-message');if(before?.parentNode)before.parentNode.insertBefore(panel,before);else document.body.append(panel);}panel.replaceChildren();const h=document.createElement('h2');h.textContent='✦ Offerta';panel.append(h);if(!state){panel.classList.add('hidden');return;}panel.classList.remove('hidden');const strip=document.createElement('div');strip.className='boss-offer';panel.append(strip);for(const inst of state.shared_mostrissimi??[]){const d=await card(inst.card_id),b=document.createElement('button');b.type='button';b.className='boss-card';b.innerHTML=cardHTML(d,true);b.setAttribute('aria-label',`Apri ${d.name}, ${d.sacrifice_cost} sacrifici`);b.disabled=busy;b.onclick=()=>inspect('boss',inst.card_id);strip.append(b);}const p=pending();if(!p)return;const info=document.createElement('p');info.className='tribute-progress';info.textContent=`Sacrifici ${p.paid.length}/${p.required}. Non puoi annullare.`;panel.append(info);if(p.paid.length<p.required){const list=document.createElement('div');list.className='tribute-list';for(const item of permanents()){const d=await card(item.card_id),b=document.createElement('button');b.type='button';b.textContent=`${item.type}: ${d.name}`;b.disabled=busy;b.onclick=()=>inspect('tribute',item.card_id,{instanceId:item.id});list.append(b);}panel.append(list);}else{const msg=document.createElement('p');msg.textContent=flow?.kind==='boss-target'?'Tocca il bersaglio ETB evidenziato.':'Tocca una cella evidenziata.';panel.append(msg);}}
function controls(){const disable=(id,v)=>{if($(id))$(id).disabled=!!v;};disable('new-match-button',busy);disable('end-turn-button',!active());disable('refresh-button',busy||!matchId);disable('direct-attack-button',!active()||flow?.kind!=='attack'||foes(flow.from).length>0);disable('cancel-selection-button',busy||!flow||!!pending());disable('choose-attack-button',true);disable('choose-move-button',true);$('creature-action-panel')?.classList.add('hidden');if($('selection-instructions'))$('selection-instructions').textContent=flow?.kind==='boss-target'?'Scegli il bersaglio ETB.':pending()?'Evocazione obbligatoria in corso.':flow?.kind==='hand'&&flow.step==='cell'?'Scegli una cella.':flow?.kind==='hand'&&flow.step==='target'?'Scegli un bersaglio.':flow?.kind==='attack'?'Scegli il nemico o attacca direttamente.':flow?.kind==='move'?'Scegli una cella adiacente.':'Tocca una miniatura per aprire la carta.';}
async function render(){const set=(id,x)=>{if($(id))$(id).textContent=String(x);};set('match-status',state?.status==='finished'?'Terminata':state?'In corso':'Nessuna partita');set('turn-status',state?`${state.current_turn} · ${state.active_player_index===1?'Tu':'IA'}`:'—');set('phase-status',state?.phase==='main'?'Principale':state?.phase??'—');set('player-life',me()?.life??20);set('player-mana',`${me()?.current_mana??0} / ${me()?.max_mana??0}`);set('player-hand-count',me()?.hand?.length??0);set('player-deck-count',me()?.deck?.length??0);set('player-graveyard-count',me()?.graveyard?.length??0);set('opponent-life',them()?.life??20);set('opponent-hand-count',them()?.hand?.length??0);set('opponent-deck-count',them()?.deck?.length??0);set('opponent-graveyard-count',them()?.graveyard?.length??0);for(const [id,owner,name] of [['player-field-spell',me(),'Tu'],['opponent-field-spell',them(),'IA']]){let text=`${name}: Nessuna Terraforma`;if(owner?.field_spell)try{text=`${name}: ${(await card(owner.field_spell.card_id)).name}`;}catch{}set(id,text);}await drawBoard();await drawHand();await drawBoss();controls();}
async function logs(){if(!$('match-logs')||!matchId)return;const {logs:entries}=await api(`/match/${matchId}/logs?limit=100`);$('match-logs').replaceChildren();for(const e of entries??[]){const li=document.createElement('li');li.textContent=e.log_data?.description??e.log_data?.action_type??'Evento';$('match-logs').append(li);}}
async function request(path,body,message){if(busy||!matchId)return;close();busy=true;controls();try{state=(await api(`/match/${matchId}/${path}`,{method:'POST',body})).state;flow=null;await render();try{await logs();}catch(e){console.warn(e);}notice(state.status==='finished'?(state.winner_index===1?'HAI VINTO!':'HAI PERSO!'):state.mostrissimo_result?.outcome==='failed'?state.mostrissimo_result.message:message,state.mostrissimo_result?.outcome==='failed'?'error':'success');}catch(e){fail(e);}finally{busy=false;await render().catch(fail);}}
async function boardClick(pos){if(!turn()||busy)return;const c=at(pos),p=pending();
 if(flow?.kind==='boss-target'){if(!p||p.paid.length!==p.required){flow=null;await render();return notice('Evocazione non più valida.','error');}const d=await card(p.card_id);if(!targetAllowed(d,c))return notice('Bersaglio ETB non valido.','error');return request('mostrissimo/complete',{position:flow.position,targetInstanceId:c.instance_id},'Mostrissimo evocato.');}
 if(p){if(p.paid.length<p.required){if(c?.owner_index===1)return inspect('tribute',c.card_id,{instanceId:c.instance_id});return notice('Scegli un tuo permanente nell’elenco dei sacrifici.','error');}if(c||!bossCells(p).some(x=>eq(x,pos)))return notice('Cella non valida.','error');const d=await card(p.card_id);if(targetEffect(d)&&targets(d).length){flow={kind:'boss-target',position:pos,id:d.id};await render();notice('Cella scelta. Tocca il bersaglio ETB evidenziato.','success');return;}return request('mostrissimo/complete',{position:pos},'Mostrissimo evocato.');}
 if(flow?.kind==='hand'){const d=await card(flow.id);if(flow.step==='cell'){if(c||pos.row!==2)return notice('Scegli una cella libera della tua riga.','error');flow.position=pos;if(targetEffect(d)&&targets(d).length){flow.step='target';await render();notice('Cella scelta. Tocca il bersaglio ETB.','success');return;}return request('play-card',{cardInstanceId:flow.instanceId,options:{position:pos}},'Creatura evocata.');}if(flow.step==='target'){if(!targetAllowed(d,c))return notice('Bersaglio non valido.','error');return request('play-card',{cardInstanceId:flow.instanceId,options:{...(flow.position?{position:flow.position}:{}),targetInstanceId:c.instance_id}},creature(d)?'Creatura evocata.':'Carta giocata.');}}
 if(flow?.kind==='move'){if(!steps(flow.from).some(x=>eq(x,pos)))return notice('Scegli una cella libera adiacente.','error');return request('move',{from:flow.from,to:pos},'Creatura mossa.');}
 if(flow?.kind==='attack'){if(!foes(flow.from).some(x=>eq(x,pos)))return notice('Scegli una creatura IA adiacente.','error');return request('attack',{attackerPosition:flow.from,target:{type:'creature',position:pos}},'Attacco risolto.');}
 if(c)return inspect('unit',c.card_id,{position:pos,cell:c});}
async function newMatch(){if(busy)return;busy=true;controls();notice('Creazione partita…');try{const result=await api('/match/create',{method:'POST',body:{}});matchId=result.match_id;state=result.state;flow=null;close();localStorage.setItem('bellum:last-match',matchId);await render();await logs();notice('Partita pronta. Tocca una carta.','success');}catch(e){fail(e);}finally{busy=false;await render().catch(fail);}}
async function refreshMatch(){if(busy||!matchId)return;busy=true;controls();try{state=(await api(`/match/${matchId}`)).state;flow=null;close();await render();await logs();notice('Aggiornato.','success');}catch(e){fail(e);}finally{busy=false;await render().catch(fail);}}
async function init(){if(initialized)return;const user=await getCurrentUser();if(!user)return;initialized=true;if($('signed-in-user'))$('signed-in-user').textContent=`@${usernameFromEmail(user.email)}`;if($('player-title'))$('player-title').textContent=usernameFromEmail(user.email)||'Tu';overlay();$('new-match-button')?.addEventListener('click',()=>newMatch().catch(fail));$('direct-attack-button')?.addEventListener('click',()=>{if(active()&&flow?.kind==='attack'&&!foes(flow.from).length)request('attack',{attackerPosition:flow.from,target:{type:'player',playerIndex:0}},'Attacco diretto risolto.');});$('cancel-selection-button')?.addEventListener('click',()=>{if(!pending()){flow=null;close();render().catch(fail);notice('Selezione annullata.');}});$('end-turn-button')?.addEventListener('click',()=>{if(active())request('end-turn',{},'È di nuovo il tuo turno.');});$('refresh-button')?.addEventListener('click',()=>refreshMatch().catch(fail));$('logout-button')?.addEventListener('click',()=>signOut().then(()=>{matchId=null;state=null;flow=null;close();}).catch(fail));document.addEventListener('keydown',e=>{if(e.key==='Escape'&&view)close();});await render();const prior=localStorage.getItem('bellum:last-match');if(prior)try{const result=await api(`/match/${prior}`);if(result.state?.state_version===2&&result.state.players?.[1]?.user_id===user.id){matchId=prior;state=result.state;await render();await logs();notice('Partita precedente ripristinata.','success');}}catch(e){console.warn('Ripristino non disponibile',e);}}
window.addEventListener('bellum:auth-ready',()=>init().catch(fail));getCurrentUser().then(user=>{if(user)return init();}).catch(fail);
